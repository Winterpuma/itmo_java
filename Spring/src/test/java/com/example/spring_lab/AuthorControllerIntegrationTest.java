/*
package com.example.spring_lab;

import com.example.spring_lab.domain.entity.Author;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.TestPropertySource;
import org.springframework.test.context.junit.jupiter.SpringExtension;
import org.springframework.test.web.servlet.MockMvc;

@ExtendWith(SpringExtension.class)
@SpringBootTest(
        webEnvironment = SpringBootTest.WebEnvironment.MOCK,
        classes = SpringLabApplication.class
)
@AutoConfigureMockMvc
@TestPropertySource(locations = "classpath:application.yml")
public class AuthorControllerIntegrationTest {
    @Autowired
    private MockMvc mvc;

    @Test
    public void givenAuthor_whenGetEmployees_thenReturnJsonArray() {
        Author author = new Author();

        author.set;


    }

    private void createTestAuthor(String name) {
        Author author = new Author(name);
        author.
        repository.saveAndFlush(emp);
    }
}
*/
